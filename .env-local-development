# MongoDB Configuration
MONGODB_URI="mongodb+srv://ijlalssck1940:ijlalssck1940@cluster0.pk4mcqx.mongodb.net/lms?retryWrites=true&w=majority&appName=Cluster0"

# JWT Configuration
JWT_SECRET=dev-secret-key-change-in-production
JWT_EXPIRATION=24h

# Server Configuration
PORT=3001

# Frontend Configuration
FRONTEND_URL=http://localhost:3000

# Optional: MongoDB Authentication (uncomment and set if needed)
# MONGODB_USER=your_username
# MONGODB_PASSWORD=your_password
